#include <iostream>
using namespace std;
/*
	字符串比较：
		* 字符串比较是按字符的ASCII码进行对比：
			= 返回   0
			> 返回   1 
			< 返回  -1

		* `int compare(const string &s) const; `  //与字符串s比较
		* `int compare(const char *s) const;`      //与字符串s比较
*/
int main() {
	system("pause");
	return 0;
}