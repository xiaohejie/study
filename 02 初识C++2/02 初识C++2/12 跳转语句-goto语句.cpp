#include <iostream>
using namespace std;
int main() {
	/*
		goto语句：
			作用：可以无条件跳转语句
			语法：goto标记
			解释：如果标记存在的话，执行到goto语句时，会跳转到标记的位置 
			（一般不建议使用goto语句，会非常影响代码的逻辑结构）
	*/
	system("pause");
	return 0;
}